function productoEntreNúmeros(a, b) {
  // Dados dos argumentos "a" y "b", devuelve el producto de todos
  // los números entre a y b (inclusive).
  // Tu código:
  for ( b = 0; b < a; b++ ){
    console.log(b);
  }
}
productoEntreNúmeros(9, 0)
module.exports = productoEntreNúmeros;