function obtenerLargoDelArray(array) {
  // Retornar la longitud del arreglo recibido.
  // Tu código:

  // F R U T A S
  //var array = ["Fresa", "Kiwi", "Uva", "Piña", "Mora"].length;
  //console.log(array);

  return array.length;
}
obtenerLargoDelArray()
module.exports = obtenerLargoDelArray;
